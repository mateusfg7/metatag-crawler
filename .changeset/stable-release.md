---
"@mateusfg7/metatag-crawler": major
---

Release the first stable version. The library is rewritten in TypeScript, ships
dual ESM + CommonJS builds with type definitions, uses `axios`, and exposes a
Promise-based `scrape(url, options?)` API.
